package com.cartrawler.assessment.car.filter;

import java.util.List;

import com.cartrawler.assessment.car.CarResult;

public interface CarResultFilter {

	void filter(List<CarResult> carResults);
}
